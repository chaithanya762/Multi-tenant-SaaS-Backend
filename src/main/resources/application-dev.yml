# Development Profile
spring:
  jpa:
    show-sql: true
  devtools:
    restart:
      enabled: true

logging:
  level:
    com.example.multitenant: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
